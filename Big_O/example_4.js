const anotherFunChallenge = (intput) => {
    let a = 5; // O(1)
    let b = 10; // O(1)
    let c = 50; // O(1)
    for (let i = 0; i < InputDeviceInfo; i++) {
        let x = i + 1;
        let y = i + 2;
        let z = i + 3;
    }
}

// big O(n)